[build]
  command = "npm run build"
  publish = "dist" # أو build حسب الإطار المستخدم

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# إعدادات الأمان للهيدرز
[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"